syntax = "proto3";
package Auth;

import "modules/types.proto";

message Coin {
  modules.CoinId id = 1;
  modules.Amount amount = 2;
}

message Account {
  repeated Coin coins = 1;
  uint64 nonce = 2; 
}